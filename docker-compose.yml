version: '3.8'

services:
  annotation-app:
    build: .
    image: annotation-app:latest
    container_name: annotation_server
    restart: unless-stopped
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      - PORT=7860
      - PYTHONUNBUFFERED=1
