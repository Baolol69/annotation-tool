version: '3.8'

services:
  annotation-app:
    build: .
    image: annotation-app:latest
    container_name: annotation_server
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
      - PYTHONUNBUFFERED=1
